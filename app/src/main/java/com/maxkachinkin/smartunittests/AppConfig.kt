package com.maxkachinkin.smartunittests

/**
 * START = false: App runs with the "bad" ViewModel (workshop starting point)
 * FINISH = true: App runs with the "good" ViewModel (workshop end result)
 */
object AppConfig {
    const val USE_FINISH = true
}
